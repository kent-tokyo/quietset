[package]
name = "quietset-py"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
name = "quietset"
crate-type = ["cdylib"]

[dependencies]
quietset = { path = "../quietset" }
pyo3 = { version = "0.21", features = ["extension-module"] }
serde_json = "1"
